---
phase: 17-dead-code-removal-and-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/security/ratelimit.go
  - internal/security/ratelimit_test.go
  - internal/security/validate.go
  - internal/security/validate_test.go
  - internal/session/session.go
  - internal/session/session_test.go
  - internal/session/store.go
  - internal/session/store_test.go
  - internal/session/persist.go
  - internal/session/persist_test.go
  - internal/session/migrate.go
  - internal/session/migrate_test.go
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-04

must_haves:
  truths:
    - "ChannelRateLimiter type does not exist in the codebase"
    - "IsAuthorized function does not exist in the codebase"
    - "internal/session directory does not exist"
    - "go build ./... succeeds with no compilation errors"
    - "go test ./internal/security/... passes"
  artifacts:
    - path: "internal/security/ratelimit.go"
      provides: "ProjectRateLimiter only — no ChannelRateLimiter"
      contains: "ProjectRateLimiter"
    - path: "internal/security/ratelimit_test.go"
      provides: "ProjectRateLimiter tests only — no Channel tests"
      contains: "TestProjectRateLimiterAllow"
    - path: "internal/security/validate.go"
      provides: "ValidatePath and CheckCommandSafety only — no IsAuthorized"
    - path: "internal/security/validate_test.go"
      provides: "ValidatePath and CheckCommandSafety tests only — no IsAuthorized tests"
  key_links:
    - from: "internal/security/ratelimit.go"
      to: "internal/dispatch/dispatcher.go"
      via: "ProjectRateLimiter import — must still compile"
      pattern: "security\\.NewProjectRateLimiter"
---

<objective>
Remove all Telegram-era dead code from the security and session packages.

Purpose: Eliminate ChannelRateLimiter, IsAuthorized, and the entire internal/session package — none are imported by production code. This closes CLEAN-02 (remove Telegram dependencies from go.mod — the types that kept channel-based concepts alive) and CLEAN-04 (session package either wired or removed — removing since dispatcher manages sessions inline).

Output: Cleaner codebase with only production-used code remaining. `go build ./...` and `go test ./internal/security/...` pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dead-code-removal-and-test-fixes/17-RESEARCH.md
@internal/security/ratelimit.go
@internal/security/ratelimit_test.go
@internal/security/validate.go
@internal/security/validate_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ChannelRateLimiter and IsAuthorized dead code</name>
  <files>internal/security/ratelimit.go, internal/security/ratelimit_test.go, internal/security/validate.go, internal/security/validate_test.go</files>
  <read_first>internal/security/ratelimit.go, internal/security/ratelimit_test.go, internal/security/validate.go, internal/security/validate_test.go</read_first>
  <action>
1. In `internal/security/ratelimit.go`:
   - Delete the `ChannelRateLimiter` struct (lines 10-17), `NewChannelRateLimiter` function (lines 21-28), and `Allow` method on `ChannelRateLimiter` (lines 33-52).
   - Remove `"time"` from the import block — it is only used by `ChannelRateLimiter.Allow` return type. The remaining `ProjectRateLimiter` does not use `time`.
   - Keep `ProjectRateLimiter` and all its methods untouched (lines 54-86).

2. In `internal/security/ratelimit_test.go`:
   - Delete `TestRateLimiterAllow` (lines 9-34), `TestRateLimiterPerChannel` (lines 36-59), `TestRateLimiterConcurrent` (lines 63-114).
   - Remove `"time"` from the import block — only used by the deleted `TestRateLimiterConcurrent`.
   - Keep `TestProjectRateLimiterAllow`, `TestProjectRateLimiterPerProject`, `TestProjectRateLimiterConcurrent` untouched (lines 116-156).
   - After deletions, check if `"sync"` is still needed (it is — used by `TestProjectRateLimiterConcurrent`).

3. In `internal/security/validate.go`:
   - Delete the `IsAuthorized` function (starts at line 37, signature: `func IsAuthorized(userID int64, channelID int64, allowedUsers []int64) bool`).
   - Check if any import becomes unused after deletion and remove it.

4. In `internal/security/validate_test.go`:
   - Delete all `IsAuthorized` tests: `TestIsAuthorizedYes`, `TestIsAuthorizedNo`, `TestIsAuthorizedChannelIDAccepted`, `TestIsAuthorizedEmptyList` (lines 82 onward in that section).
   - Keep all `ValidatePath` and `CheckCommandSafety` tests.

5. Run `go build ./internal/security/...` to confirm no compilation errors from stale imports.
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-node && go build ./internal/security/... && go test ./internal/security/... -v 2>&1 | head -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "ChannelRateLimiter" internal/security/` returns no matches
    - `grep -r "IsAuthorized" internal/security/` returns no matches
    - `go build ./internal/security/...` exits 0
    - `go test ./internal/security/...` passes with TestProjectRateLimiterAllow, TestProjectRateLimiterPerProject, TestProjectRateLimiterConcurrent, TestValidatePathAllowed, TestValidatePathBlocked, TestValidatePathNormalization, TestValidatePathWindows all passing
  </acceptance_criteria>
  <done>ChannelRateLimiter type+constructor+method deleted. IsAuthorized function deleted. All associated tests deleted. ProjectRateLimiter and ValidatePath tests still pass. No unused imports.</done>
</task>

<task type="auto">
  <name>Task 2: Remove internal/session package entirely</name>
  <files>internal/session/session.go, internal/session/session_test.go, internal/session/store.go, internal/session/store_test.go, internal/session/persist.go, internal/session/persist_test.go, internal/session/migrate.go, internal/session/migrate_test.go</files>
  <read_first>internal/session/session.go, internal/session/store.go, internal/session/persist.go, internal/session/migrate.go</read_first>
  <action>
1. Verify no production code imports `internal/session`:
   - Run `grep -r '"github.com/user/gsd-tele-go/internal/session"' internal/ cmd/ --include='*.go'` excluding the session package itself.
   - Expected: zero matches (confirmed by research).

2. Delete the entire `internal/session/` directory:
   - `rm -rf internal/session/`

3. Run `go build ./...` to confirm nothing breaks.

The dispatcher manages session IDs directly via `proc.SessionID()` in `runInstance()`. The session package was Telegram-era scaffolding (one session per chat) that was partially migrated in Phase 12 but never wired into production. Removing it is the correct resolution per CLEAN-04 ("wired into production code at startup OR removed entirely").
  </action>
  <verify>
    <automated>cd /Users/josh/code/glsd-node && test ! -d internal/session && go build ./... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d internal/session` exits 0 (directory does not exist)
    - `go build ./...` exits 0 (no broken imports)
    - `grep -r 'internal/session' internal/ cmd/ --include='*.go'` returns no matches
  </acceptance_criteria>
  <done>internal/session/ directory fully removed. No production code references it. go build ./... succeeds.</done>
</task>

</tasks>

<verification>
- `go build ./...` passes (no broken imports from deletions)
- `go test ./internal/security/...` passes (ProjectRateLimiter + ValidatePath + CheckCommandSafety tests green)
- `grep -r "ChannelRateLimiter\|IsAuthorized" internal/` returns no matches
- `test ! -d internal/session` confirms session package removed
</verification>

<success_criteria>
All Telegram-era dead code removed: ChannelRateLimiter, IsAuthorized, and internal/session package. Codebase compiles and security tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/17-dead-code-removal-and-test-fixes/17-01-SUMMARY.md`
</output>
