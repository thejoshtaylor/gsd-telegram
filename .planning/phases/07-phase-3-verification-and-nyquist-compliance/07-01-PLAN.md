---
phase: 07-phase-3-verification-and-nyquist-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md
autonomous: true
requirements:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
  - DEPLOY-02

must_haves:
  truths:
    - "03-VERIFICATION.md exists with status: passed or human_needed"
    - "All 6 requirements (MEDIA-01..05, DEPLOY-02) appear in Requirements Coverage table with SATISFIED status"
    - "Observable Truths table has at least 10 rows with VERIFIED status covering voice, photo, album, PDF, text file, and Windows Service"
    - "Key Link Verification table confirms dispatcher wiring from bot/handlers.go to each media handler"
    - "Build and Test Results section shows go test ./... all packages PASS"
  artifacts:
    - path: ".planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md"
      provides: "Phase 3 formal verification report"
      min_lines: 80
      contains: "Observable Truths"
  key_links:
    - from: "03-VERIFICATION.md Observable Truths"
      to: "internal/handlers/voice.go, photo.go, document.go"
      via: "line-by-line source inspection evidence"
      pattern: "VERIFIED"
---

<objective>
Write 03-VERIFICATION.md: formal verification report for Phase 3 (Media Handlers and Windows Service).

Purpose: Phase 3 was executed (all 4 plans complete) but never formally verified. This creates the verification artifact that confirms all 6 requirements are satisfied, following the exact format established by 04-VERIFICATION.md.

Output: `.planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md`
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase-3-verification-and-nyquist-compliance/07-RESEARCH.md

<interfaces>
<!-- Template format from 04-VERIFICATION.md (the reference implementation) -->
Frontmatter: phase, verified, status, score, re_verification
Sections: Goal Achievement > Observable Truths table > Required Artifacts > Key Link Verification > Requirements Coverage > Anti-Patterns Found > Human Verification Required > Build and Test Results > Gaps Summary

<!-- Source files to inspect for evidence -->
internal/handlers/voice.go — HandleVoice, transcribeVoice, downloadToTemp
internal/handlers/photo.go — HandlePhoto, buildSinglePhotoPrompt, buildAlbumPrompt
internal/handlers/document.go — HandleDocument, classifyDocument, isTextFile
internal/handlers/helpers.go — extractPDF, downloadToTemp, transcribeVoice
internal/handlers/media_group.go — MediaGroupBuffer, Add, flush
internal/bot/handlers.go — dispatcher wiring (handleVoice, handlePhoto, handleDocument)
docs/windows-service.md — NSSM installation guide
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite and inspect source files for evidence</name>
  <files>.planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-callback-handler-integration-fixes/04-VERIFICATION.md (template format)
    - .planning/phases/07-phase-3-verification-and-nyquist-compliance/07-RESEARCH.md (pre-verified evidence strings)
    - internal/handlers/voice.go
    - internal/handlers/photo.go
    - internal/handlers/document.go
    - internal/handlers/helpers.go
    - internal/handlers/media_group.go
    - internal/bot/handlers.go
    - docs/windows-service.md
    - internal/handlers/voice_test.go
    - internal/handlers/photo_test.go
    - internal/handlers/document_test.go
    - internal/handlers/media_group_test.go
    - internal/handlers/helpers_test.go
  </read_first>
  <action>
1. Run the full test suite: `go test ./... -count=1` — capture output including package pass/fail and test counts.
2. Run targeted handler tests: `go test ./internal/handlers/... -v -count=1` — capture verbose output with individual test names and PASS/FAIL.
3. Inspect each source file line-by-line and record evidence for each Observable Truth. The 07-RESEARCH.md has pre-verified evidence strings — cross-check each against the actual source:

Observable Truths to verify (minimum 12 rows):
- MEDIA-01 T1: voice.go has `if cfg.OpenAIAPIKey == ""` guard returning friendly error
- MEDIA-01 T2: voice.go calls downloadToTemp then transcribeVoice with context.WithTimeout 60s
- MEDIA-01 T3: voice.go enqueues transcript to Claude session via store.GetOrCreate + sess.Enqueue
- MEDIA-02 T1: photo.go selects largest PhotoSize via `photos[len(photos)-1]` and calls downloadToTemp with .jpg suffix
- MEDIA-02 T2: photo.go buildSinglePhotoPrompt produces `[Photo: /path]` format (confirmed by TestBuildSinglePhotoPrompt)
- MEDIA-03 T1: media_group.go MediaGroupBuffer.Add resets timer per item using time.AfterFunc + sync.Mutex
- MEDIA-03 T2: photo.go buildAlbumPrompt produces `[Photos:\n1. ...\n2. ...]` format (confirmed by TestBuildAlbumPrompt)
- MEDIA-04 T1: helpers.go extractPDF runs `pdftotext -layout <file> -` and returns partial output on non-zero exit
- MEDIA-04 T2: document.go classifyDocument routes .pdf extension to PDF extraction (confirmed by TestClassifyDocument 12-case test)
- MEDIA-05 T1: document.go isTextFile has 17-entry extension map (confirmed by TestIsTextFile_Extensions)
- MEDIA-05 T2: document.go enforces maxFileSize=10*1024*1024 and maxTextChars=100_000 (confirmed by TestConstants_Helpers)
- DEPLOY-02 T1: docs/windows-service.md exists with 5 sections: Prerequisites, Install, Manage, Env Vars, Troubleshooting

Required Artifacts to verify (5 entries):
- internal/handlers/voice.go — HandleVoice function exists, not a stub (check line count > 50)
- internal/handlers/photo.go — HandlePhoto function exists, not a stub (check line count > 50)
- internal/handlers/document.go — HandleDocument function exists, not a stub (check line count > 50)
- internal/handlers/media_group.go — MediaGroupBuffer type with Add/flush methods (check line count > 30)
- docs/windows-service.md — NSSM documentation with all 5 sections

Key Links to verify (4 entries):
- internal/bot/handlers.go -> internal/handlers/voice.go via `handlers.NewMessage(message.Voice, b.handleVoice)` dispatcher line
- internal/bot/handlers.go -> internal/handlers/photo.go via `handlers.NewMessage(message.Photo, b.handlePhoto)` dispatcher line
- internal/bot/handlers.go -> internal/handlers/document.go via `handlers.NewMessage(message.Document, b.handleDocument)` dispatcher line
- bot wrapper functions pass full parameter set including auditLog, persist, WaitGroup(), globalAPILimiter

Requirements Coverage (6 entries):
- MEDIA-01: SATISFIED — voice handler with Whisper transcription, no-API-key guard, session enqueue
- MEDIA-02: SATISFIED — photo handler with largest-size selection, temp file download, Claude prompt
- MEDIA-03: SATISFIED — MediaGroupBuffer with timer reset, album prompt format
- MEDIA-04: SATISFIED — PDF extraction via pdftotext, partial output on error, classifyDocument routing
- MEDIA-05: SATISFIED — text file extension map, content reading, size limit enforcement
- DEPLOY-02: SATISFIED (HUMAN_NEEDED for operational verification) — docs/windows-service.md exists

Human Verification Required section must include 3 items (from 03-VALIDATION.md Manual-Only list):
1. NSSM service installs and starts at boot (DEPLOY-02)
2. Voice message transcription end-to-end (MEDIA-01)
3. Photo album rendering in Claude (MEDIA-03)

4. Write 03-VERIFICATION.md using the exact format from 04-VERIFICATION.md:
- Frontmatter: `phase: 03-media-handlers-and-windows-service`, `verified: 2026-03-20T00:00:00Z`, `status: human_needed` (because DEPLOY-02 and live media tests require manual verification), `score: 12/12 must-haves verified`, `re_verification: false`
- Include all sections: Observable Truths, Required Artifacts, Key Link Verification, Requirements Coverage, Anti-Patterns Found, Human Verification Required, Build and Test Results, Gaps Summary
- Evidence strings must reference actual line numbers from the source files (not SUMMARY.md)
- Anti-Patterns section: scan voice.go, photo.go, document.go, helpers.go, media_group.go for TODOs, FIXMEs, placeholder returns, stub handlers
  </action>
  <verify>
    <automated>test -f .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md && grep -c "VERIFIED" .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md exists
    - File contains `status: human_needed` in frontmatter
    - File contains `score: 12/12 must-haves verified` in frontmatter (or actual count if different)
    - File contains at least 10 rows in Observable Truths table with `VERIFIED` status
    - File contains `MEDIA-01` through `MEDIA-05` and `DEPLOY-02` in Requirements Coverage table
    - File contains `Build and Test Results` section with `go test ./...` output
    - File contains `Human Verification Required` section with at least 3 items
    - File contains `Key Link Verification` section with at least 3 WIRED entries
    - grep "SATISFIED" 03-VERIFICATION.md returns at least 6 matches
  </acceptance_criteria>
  <done>03-VERIFICATION.md written with all Observable Truths verified against actual source lines, all 6 requirements covered in Requirements Coverage, key links confirmed wired, and human verification items documented for DEPLOY-02 and live media tests</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md` exits 0
- `grep -c "VERIFIED" .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md` returns >= 10
- `grep -c "SATISFIED" .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md` returns >= 6
- `grep "status:" .planning/phases/03-media-handlers-and-windows-service/03-VERIFICATION.md` shows `human_needed`
</verification>

<success_criteria>
03-VERIFICATION.md exists with formal verification of all 6 Phase 3 requirements, evidence traced to actual source line numbers, test suite results included, and human verification items documented.
</success_criteria>

<output>
After completion, create `.planning/phases/07-phase-3-verification-and-nyquist-compliance/07-01-SUMMARY.md`
</output>
