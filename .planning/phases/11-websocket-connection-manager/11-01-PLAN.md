---
phase: 11-websocket-connection-manager
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/protocol/messages.go
  - internal/protocol/messages_test.go
  - internal/connection/manager.go
  - internal/connection/dial.go
  - internal/connection/writer.go
  - internal/connection/backoff.go
  - internal/connection/manager_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [XPORT-01, XPORT-06]

must_haves:
  truths:
    - "ConnectionManager dials outbound to a wss:// URL with Bearer token auth"
    - "All outbound WebSocket writes go through a single writer goroutine via Send()"
    - "Send() returns ErrStopped after the manager is stopped"
    - "Dial uses exponential backoff with jitter (500ms-30s) on failure"
  artifacts:
    - path: "internal/connection/manager.go"
      provides: "ConnectionManager struct, NewConnectionManager, Start, Stop, Send public API"
      exports: ["ConnectionManager", "NewConnectionManager", "ErrStopped"]
    - path: "internal/connection/dial.go"
      provides: "dialLoop with backoff and reconnect skeleton"
    - path: "internal/connection/writer.go"
      provides: "Single writer goroutine draining sendCh"
    - path: "internal/connection/backoff.go"
      provides: "backoffState with full jitter"
    - path: "internal/protocol/messages.go"
      provides: "TypeNodeDisconnect constant and NodeDisconnect struct"
      contains: "TypeNodeDisconnect"
  key_links:
    - from: "internal/connection/manager.go"
      to: "internal/config/node_config.go"
      via: "cfg *config.NodeConfig parameter"
      pattern: "config\\.NodeConfig"
    - from: "internal/connection/dial.go"
      to: "github.com/coder/websocket"
      via: "websocket.Dial call"
      pattern: "websocket\\.Dial"
    - from: "internal/connection/writer.go"
      to: "internal/connection/manager.go"
      via: "drains sendCh channel"
      pattern: "m\\.sendCh"
---

<objective>
Build the ConnectionManager core: outbound WebSocket dial with Bearer token auth, exponential backoff on failures, and a single writer goroutine that serializes all outbound frames. Also add the missing TypeNodeDisconnect to the protocol package and install coder/websocket + goleak dependencies.

Purpose: This is the transport foundation. Every downstream phase (heartbeat, registration, dispatch, instance management) calls manager.Send() to write frames. The single-writer guarantee (XPORT-06) and outbound dial (XPORT-01) must be solid before layering on lifecycle behavior.

Output: Compilable `internal/connection/` package with working dial, backoff, writer, and mock-server-based tests. Protocol package extended with disconnect type.
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-websocket-connection-manager/11-RESEARCH.md

<interfaces>
<!-- From internal/protocol/messages.go — envelope encoding used by connection manager -->
```go
const (
    TypeNodeRegister     = "node_register"
    TypeExecute          = "execute"
    TypeKill             = "kill"
    TypeStatusRequest    = "status_request"
    TypeStreamEvent      = "stream_event"
    TypeInstanceStarted  = "instance_started"
    TypeInstanceFinished = "instance_finished"
    TypeInstanceError    = "instance_error"
)

type Envelope struct {
    Type    string          `json:"type"`
    ID      string          `json:"id"`
    Payload json.RawMessage `json:"payload,omitempty"`
}

func Encode(msgType, msgID string, payload any) (Envelope, error)
func (e *Envelope) Decode(dst any) error

type NodeRegister struct {
    NodeID           string            `json:"node_id"`
    Platform         string            `json:"platform"`
    Version          string            `json:"version"`
    Projects         []string          `json:"projects"`
    RunningInstances []InstanceSummary `json:"running_instances"`
}
```

<!-- From internal/config/node_config.go — config consumed by connection manager -->
```go
type NodeConfig struct {
    ServerURL             string
    ServerToken           string
    HeartbeatIntervalSecs int
    NodeID                string
}

func LoadNodeConfig() (*NodeConfig, error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypeNodeDisconnect to protocol and install dependencies</name>
  <files>internal/protocol/messages.go, internal/protocol/messages_test.go, go.mod, go.sum</files>
  <read_first>
    - internal/protocol/messages.go (current message types and const block)
    - internal/protocol/messages_test.go (existing test patterns)
    - go.mod (current dependencies)
  </read_first>
  <action>
1. Add `TypeNodeDisconnect = "node_disconnect"` to the const block in `internal/protocol/messages.go`, after `TypeInstanceError`.

2. Add a `NodeDisconnect` struct below the other outbound structs:
```go
// NodeDisconnect is sent by the node before a clean WebSocket close.
// Reason is optional — empty string means normal shutdown.
type NodeDisconnect struct {
    Reason string `json:"reason,omitempty"`
}
```

3. Add a round-trip test in `internal/protocol/messages_test.go` for NodeDisconnect (follow the existing test pattern — encode, decode, assert fields match).

4. Install dependencies:
```bash
go get github.com/coder/websocket@v1.8.14
go get go.uber.org/goleak
go mod tidy
```

5. Verify: `go test ./internal/protocol/... -timeout 30s` passes, `go mod tidy` shows no diff.
  </action>
  <verify>
    <automated>go test ./internal/protocol/... -timeout 30s</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'TypeNodeDisconnect.*=.*"node_disconnect"' internal/protocol/messages.go
    - grep -q 'type NodeDisconnect struct' internal/protocol/messages.go
    - grep -q 'coder/websocket' go.mod
    - grep -q 'goleak' go.mod
    - go test ./internal/protocol/... -timeout 30s exits 0
  </acceptance_criteria>
  <done>TypeNodeDisconnect constant and NodeDisconnect struct exist in protocol package with passing round-trip test. coder/websocket v1.8.14 and goleak are in go.mod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ConnectionManager core with dial, backoff, and single writer</name>
  <files>internal/connection/manager.go, internal/connection/dial.go, internal/connection/writer.go, internal/connection/backoff.go, internal/connection/manager_test.go</files>
  <read_first>
    - internal/config/node_config.go (NodeConfig fields the manager consumes)
    - internal/protocol/messages.go (Envelope, Encode, NodeRegister, NodeDisconnect types)
    - .planning/phases/11-websocket-connection-manager/11-RESEARCH.md (patterns 1-6, anti-patterns, pitfalls)
  </read_first>
  <behavior>
    - Test: NewConnectionManager returns non-nil manager with sendCh and stopCh initialized
    - Test: manager.Send(data) after Stop() returns ErrStopped
    - Test: manager dials mock TLS server, server receives the WebSocket upgrade (TestDial)
    - Test: dial with unreachable server retries with backoff; backoff values are in 0..current range (TestBackoff)
    - Test: 10 goroutines calling Send() concurrently complete without panic under -race (TestConcurrentSend)
    - Test: goleak.VerifyNone confirms no goroutine leaks after Stop()
  </behavior>
  <action>
1. Create `internal/connection/backoff.go`:
   - `type backoffState struct { current, min, max time.Duration }`
   - `func newBackoff(min, max time.Duration) *backoffState`
   - `func (b *backoffState) Next() time.Duration` — full jitter: `rand.Int63n(int64(b.current) + 1)`, then double current up to max
   - `func (b *backoffState) Reset()` — set current back to min

2. Create `internal/connection/manager.go`:
   - Package `connection`
   - `var ErrStopped = errors.New("connection manager stopped")`
   - `type ConnectionManager struct`:
     - `cfg *config.NodeConfig`
     - `log zerolog.Logger`
     - `sendCh chan []byte` (buffered 64)
     - `recvCh chan *protocol.Envelope` (buffered 64, for Phase 13 to consume)
     - `stopCh chan struct{}`
     - `stopped chan struct{}`
     - `cancel context.CancelFunc`
   - `func NewConnectionManager(cfg *config.NodeConfig, log zerolog.Logger) *ConnectionManager` — initialize all channels
   - `func (m *ConnectionManager) Start(ctx context.Context)` — creates child context, launches `go m.dialLoop(childCtx)` in a goroutine, returns immediately
   - `func (m *ConnectionManager) Stop()` — calls `m.cancel()`, closes `m.stopCh`, waits on `<-m.stopped`
   - `func (m *ConnectionManager) Send(data []byte) error` — select on sendCh vs stopped; return ErrStopped if stopped
   - `func (m *ConnectionManager) Receive() <-chan *protocol.Envelope` — returns recvCh

3. Create `internal/connection/dial.go`:
   - `func (m *ConnectionManager) dialLoop(ctx context.Context)` — for-loop that:
     - Calls `websocket.Dial(ctx, m.cfg.ServerURL, &websocket.DialOptions{HTTPHeader: http.Header{"Authorization": {"Bearer " + m.cfg.ServerToken}}, HTTPClient: m.httpClient})`
     - On error: log warning, sleep `backoff.Next()` with ctx select, continue
     - On success: `backoff.Reset()`, call `m.handleConn(ctx, conn)` (blocks until conn dies)
     - After handleConn returns: check `ctx.Done()`, if not done loop back to reconnect
     - On exit: close `m.stopped`
   - Add `httpClient *http.Client` field to ConnectionManager (nil for production, set to `srv.Client()` in tests for TLS cert trust)
   - Export `func (m *ConnectionManager) SetHTTPClient(c *http.Client)` for test injection

4. Create `internal/connection/writer.go`:
   - `func (m *ConnectionManager) runWriter(ctx context.Context, conn *websocket.Conn)` — select loop draining sendCh, calling `conn.Write(ctx, websocket.MessageText, data)`, cancels context on write error
   - This goroutine is the ONLY code path that calls `conn.Write` (XPORT-06)

5. Create `internal/connection/manager.go` `handleConn` method (stub for Plan 01 — Plan 02 adds heartbeat and registration):
   - `func (m *ConnectionManager) handleConn(ctx context.Context, conn *websocket.Conn)`:
     - `defer conn.CloseNow()`
     - Create `connCtx, cancel := context.WithCancel(ctx)`, defer cancel
     - Start reader goroutine: loop `conn.Read(connCtx)`, decode envelope, non-blocking send to `m.recvCh`, cancel on error
     - Start writer goroutine via `m.runWriter(connCtx, conn)`
     - `wg.Wait()`

6. Create `internal/connection/manager_test.go` with tests:
   - `func newMockServer(t *testing.T, handler func(*websocket.Conn)) (string, *httptest.Server)` — creates TLS server with websocket.Accept, passes conn to handler
   - `TestNewConnectionManager` — constructor returns non-nil, sendCh works
   - `TestSendAfterStop` — Start then Stop, Send returns ErrStopped
   - `TestDial` — mock server accepts connection, test verifies connection established by reading a frame sent via Send()
   - `TestBackoff` — unit test backoffState: Next() returns values in [0, current], doubles up to max, Reset() works
   - `TestConcurrentSend` — 10 goroutines send 100 frames each; mock server reads all 1000 frames; no panic under -race
   - Every test: `defer goleak.VerifyNone(t)` (except TestBackoff which has no goroutines)
  </action>
  <verify>
    <automated>go test -race ./internal/connection/... -timeout 60s</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'type ConnectionManager struct' internal/connection/manager.go
    - grep -q 'var ErrStopped' internal/connection/manager.go
    - grep -q 'func (m \*ConnectionManager) Send' internal/connection/manager.go
    - grep -q 'func (m \*ConnectionManager) dialLoop' internal/connection/dial.go
    - grep -q 'func (m \*ConnectionManager) runWriter' internal/connection/writer.go
    - grep -q 'func newBackoff' internal/connection/backoff.go
    - grep -q 'func TestDial' internal/connection/manager_test.go
    - grep -q 'func TestConcurrentSend' internal/connection/manager_test.go
    - grep -q 'goleak.VerifyNone' internal/connection/manager_test.go
    - go test -race ./internal/connection/... -timeout 60s exits 0
  </acceptance_criteria>
  <done>ConnectionManager dials a mock TLS server with Bearer auth, sends frames through a single writer goroutine, retries with exponential backoff on failure, and all tests pass under -race with no goroutine leaks.</done>
</task>

</tasks>

<verification>
- `go test ./internal/protocol/... -timeout 30s` — protocol tests pass with new NodeDisconnect type
- `go test -race ./internal/connection/... -timeout 60s` — all connection tests pass, race-clean
- `go vet ./internal/connection/...` — no vet warnings
- `grep -c 'conn.Write' internal/connection/` — only writer.go calls conn.Write (XPORT-06)
</verification>

<success_criteria>
- ConnectionManager can dial a wss:// endpoint with Bearer token authentication
- All writes are serialized through a single writer goroutine (only writer.go calls conn.Write)
- Backoff on dial failure follows 500ms-30s range with jitter
- Send() returns ErrStopped after Stop() is called
- 10-goroutine concurrent send stress test passes under -race
- No goroutine leaks detected by goleak
</success_criteria>

<output>
After completion, create `.planning/phases/11-websocket-connection-manager/11-01-SUMMARY.md`
</output>
