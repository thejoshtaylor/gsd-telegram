---
phase: 02-multi-project-and-gsd-integration
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - internal/handlers/gsd_test.go
  - internal/handlers/callback_test.go
  - internal/bot/bot.go
autonomous: false
requirements: [PROJ-04, GSD-01, GSD-05]

must_haves:
  truths:
    - "All tests pass across the full project"
    - "Bot compiles and can be started"
    - "User can visually verify the GSD keyboard and multi-project flow"
  artifacts:
    - path: "internal/handlers/gsd_test.go"
      provides: "End-to-end tests for GSD keyboard and callback integration"
    - path: "internal/handlers/callback_test.go"
      provides: "Full callback routing coverage"
  key_links:
    - from: "internal/bot/handlers.go"
      to: "internal/handlers/command.go"
      via: "/gsd and /project command registration"
      pattern: "handleGsd|handleProject"
---

<objective>
Run the full test suite, fix any integration issues, and present the bot for human verification of the multi-project and GSD keyboard flows.

Purpose: Catch integration issues between Plans 01-03 and verify the UX end-to-end.

Output: All tests green, bot compiles, human verification of key flows.
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-project-and-gsd-integration/02-CONTEXT.md
@.planning/phases/02-multi-project-and-gsd-integration/02-01-SUMMARY.md
@.planning/phases/02-multi-project-and-gsd-integration/02-02-SUMMARY.md
@.planning/phases/02-multi-project-and-gsd-integration/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + integration fix-up</name>
  <files>internal/handlers/gsd_test.go, internal/handlers/callback_test.go, internal/bot/bot.go</files>
  <read_first>
    - internal/handlers/gsd_test.go (current test state)
    - internal/handlers/callback_test.go (current test state)
    - internal/handlers/text_test.go (current test state — added in Plan 02)
    - internal/bot/bot.go (verify all fields initialized)
    - internal/bot/handlers.go (verify all commands registered)
    - internal/handlers/text.go (verify HandleText signature matches wrapper)
    - internal/handlers/command.go (verify HandleGsd, HandleProject, HandleResume signatures)
    - internal/handlers/callback.go (verify HandleCallback signature)
  </read_first>
  <action>
Run `go build ./...` and `go test ./... -count=1`. Fix any compilation errors or test failures.

Common integration issues to check:
1. HandleText signature in text.go matches the wrapper call in handlers.go (all parameters align, including globalLimiter)
2. HandleCallback signature in callback.go matches the wrapper call in handlers.go (includes mappings, awaitingPath)
3. HandleGsd and HandleProject signatures match their wrappers
4. HandleResume signature includes mappings parameter and wrapper passes b.mappings
5. All imports are correct (no circular dependencies between handlers and project packages)
6. The `AwaitingPathState` type is exported (capital A) and accessible from bot package
7. The `NewStreamingState` call passes the globalLimiter parameter
8. `config.BuildSafetyPrompt` (exported) is used everywhere (not the old unexported name)
9. `sess.WorkerStarted()` and `sess.SetWorkerStarted()` are used consistently
10. Per-project session persistence: OnQueryComplete uses mapping.Path as WorkingDir
11. /resume filters sessions by mapping.Path

If any test failures, fix the root cause. If compilation errors from signature mismatches between plans, align them.

Run final: `go build ./... && go test ./... -count=1 && go vet ./...`
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele && go build ./... && go test ./... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `go test ./... -count=1` exits 0
    - `go vet ./...` exits 0
    - No compilation errors in any package
  </acceptance_criteria>
  <done>Full project compiles, all tests pass, go vet clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify multi-project and GSD keyboard flows</name>
  <what-built>
Multi-project channel isolation and GSD inline keyboard workflow:
- Channel-to-project mapping with JSON persistence
- Free-text path entry for new channels
- /project command with Change/Unlink buttons
- Per-project session persistence (each project keeps 5 max sessions; /resume shows only current project's sessions)
- /gsd command with 20-operation categorized keyboard
- Phase picker for phase-specific operations
- Response button extraction (GSD commands and numbered/lettered options)
- Global API rate limiter for multi-channel streaming safety
- ask_user MCP callback integration
  </what-built>
  <how-to-verify>
1. Start the bot: `cd C:/DATA/CODE/gsd-tele && go run .` (or `go build -o gsd-tele.exe . && ./gsd-tele.exe`)
2. Send a message in a new/unlinked channel — bot should prompt "Reply with the full directory path to link it."
3. Reply with a valid project directory path — bot should confirm "Linked to {path}."
4. Send a text message — Claude should respond (verify per-project isolation)
5. Type `/project` — should show current mapping with Change/Unlink buttons
6. Type `/gsd` — should show status header + 20-operation keyboard (Next/Progress on top row)
7. Tap "Next" — should send `/gsd:next` to Claude
8. Tap a phase-picker operation (e.g. "Execute Phase") — should show phase picker with status indicators
9. Verify responses with GSD commands show Run/Fresh buttons
10. Type `/resume` — should show only sessions for the current project (not sessions from other projects)
11. If running two channels simultaneously, verify no Telegram 429 errors in logs
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
```bash
cd C:/DATA/CODE/gsd-tele && go build ./... && go test ./... -count=1
```
</verification>

<success_criteria>
- Full test suite passes
- Bot compiles and starts
- Human verification confirms multi-project and GSD keyboard flows work
- /resume shows only current project's sessions
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-project-and-gsd-integration/02-04-SUMMARY.md`
</output>
