---
phase: 01-core-bot-infrastructure
plan: 08
type: execute
wave: 4
depends_on: [01-06, 01-07]
files_modified:
  - main.go
  - .gitignore
autonomous: false
requirements: [DEPLOY-01, DEPLOY-04]

must_haves:
  truths:
    - "Go binary compiles for Windows"
    - "Bot starts, connects to Telegram, and processes a text message"
    - "Bot shuts down gracefully on interrupt signal"
  artifacts:
    - path: "main.go"
      provides: "Application entry point"
      contains: "func main()"
    - path: ".gitignore"
      provides: "Git ignore for build artifacts and data"
      contains: "*.exe"
  key_links:
    - from: "main.go"
      to: "internal/bot/bot.go"
      via: "bot.New and bot.Start"
      pattern: "bot\\.New"
    - from: "main.go"
      to: "internal/config/config.go"
      via: "config.Load at startup"
      pattern: "config\\.Load"
---

<objective>
Create the main.go entry point, verify the full build compiles, and run a smoke test.

Purpose: This is the final wiring step that makes the bot a runnable binary. Verifies all packages integrate correctly.
Output: main.go entry point, .gitignore, compilable binary
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-bot-infrastructure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main.go entry point and .gitignore</name>
  <files>main.go, .gitignore</files>
  <read_first>
    - internal/bot/bot.go (Bot, New, Start, Stop)
    - internal/config/config.go (Config, Load)
    - src/index.ts (TypeScript entry point — startup sequence)
  </read_first>
  <action>
1. Create `main.go`:

   ```go
   package main
   ```

   `func main()`:
   - Initialize zerolog: `zerolog.TimeFieldFormat = zerolog.TimeFormatUnix`, set up console writer for dev
   - Call `config.Load()` — if error, log fatal and exit
   - Log startup: bot token (masked: first 5 chars + "..."), working dir, claude path, allowed users count
   - Create data directory: `os.MkdirAll(cfg.DataDir, 0755)`
   - Call `bot.New(cfg)` — if error, log fatal
   - Set up signal handling: `signal.Notify(sigCh, syscall.SIGINT, syscall.SIGTERM)`
   - Start bot in goroutine: `go func() { if err := b.Start(ctx); err != nil { log.Fatal().Err(err).Msg("Bot failed") } }()`
   - Block on signal channel
   - On signal received: log "Received shutdown signal", call `b.Stop()`
   - Exit cleanly

   Signal handling must support graceful shutdown (DEPLOY-04):
   - Create context with cancel
   - On SIGINT/SIGTERM: cancel context, which propagates to all session workers
   - bot.Stop() waits for workers via WaitGroup

2. Create/update `.gitignore`:
   ```
   # Build artifacts
   *.exe
   gsd-tele-go
   gsd-tele-go.exe

   # Environment
   .env

   # Runtime data
   data/session-history.json
   data/channel-projects.json

   # IDE
   .idea/
   .vscode/

   # OS
   .DS_Store
   Thumbs.db
   ```

3. Verify full build:
   - `go build -o gsd-tele-go.exe .` (Windows)
   - `go vet ./...`
   - `go test ./... -count=1`
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele &amp;&amp; go build -o gsd-tele-go.exe . &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - main.go contains `func main()`
    - main.go contains `config.Load()`
    - main.go contains `bot.New(`
    - main.go contains `bot.Start(`
    - main.go contains `signal.Notify`
    - main.go contains `syscall.SIGINT`
    - main.go contains `syscall.SIGTERM`
    - .gitignore contains `*.exe`
    - .gitignore contains `.env`
    - .gitignore contains `data/session-history.json`
    - `go build -o gsd-tele-go.exe .` exits 0 (binary produced)
    - `go vet ./...` exits 0
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <done>main.go wires config + bot, handles signals for graceful shutdown, binary compiles for Windows, all vet and tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test the running bot end-to-end</name>
  <files>main.go</files>
  <action>
Human verifies the complete bot works end-to-end by running it with a real Telegram token and Claude CLI.
  </action>
  <verify>Manual verification per steps below</verify>
  <done>Bot starts, receives text, streams Claude response, all commands work, shuts down cleanly</done>
  <what-built>Complete Go bot that accepts text messages, routes to Claude CLI, streams responses back with MarkdownV2 formatting, and supports /start /new /stop /status /resume commands</what-built>
  <how-to-verify>
    1. Create a `.env` file with your TELEGRAM_BOT_TOKEN, TELEGRAM_ALLOWED_USERS, and CLAUDE_CLI_PATH
    2. Run: `go run .`
    3. Verify startup log shows: bot username, claude path resolved, "Bot started"
    4. Send a text message to the bot — verify streaming response appears with live updates
    5. Run `/status` — verify dashboard shows session info, tokens, context percentage
    6. Run `/stop` while a query is running — verify it stops
    7. Run `/new` — verify new session message
    8. Run `/resume` — verify inline keyboard with saved sessions
    9. Press Ctrl+C — verify "Shutting down" and clean exit
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `go build -o gsd-tele-go.exe .` produces binary
- `go vet ./...` clean
- `go test ./... -race -count=1` all green
- Manual smoke test confirms end-to-end text -> Claude -> streaming response
</verification>

<success_criteria>
- Binary compiles for Windows
- Bot starts, connects to Telegram, processes text messages with streaming
- All commands work per CONTEXT.md specifications
- Graceful shutdown on SIGINT/SIGTERM
- All tests pass with race detector
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-bot-infrastructure/01-08-SUMMARY.md`
</output>
