---
phase: 01-core-bot-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/security/ratelimit.go
  - internal/security/ratelimit_test.go
  - internal/security/validate.go
  - internal/security/validate_test.go
autonomous: true
requirements: [CORE-05, AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Rate limiter allows/blocks per channel using token bucket"
    - "Path validation blocks access outside allowed directories"
    - "Command safety check blocks dangerous patterns"
    - "Auth check validates user against allowed user list"
  artifacts:
    - path: "internal/security/ratelimit.go"
      provides: "Per-channel token bucket rate limiter"
      exports: ["ChannelRateLimiter", "NewChannelRateLimiter"]
    - path: "internal/security/validate.go"
      provides: "Path validation and command safety"
      exports: ["ValidatePath", "CheckCommandSafety", "IsAuthorized"]
  key_links:
    - from: "internal/security/ratelimit.go"
      to: "golang.org/x/time/rate"
      via: "token bucket implementation"
      pattern: "rate\\.NewLimiter"
    - from: "internal/security/validate.go"
      to: "filepath.Clean"
      via: "path normalization"
      pattern: "filepath\\.Clean"
---

<objective>
Build the security subsystem — rate limiting, path validation, command safety, and auth.

Purpose: All incoming messages pass through these checks before reaching Claude. Must be correct and goroutine-safe.
Output: internal/security/ package with rate limiter and validation
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-bot-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-channel rate limiter</name>
  <files>internal/security/ratelimit.go, internal/security/ratelimit_test.go</files>
  <read_first>
    - src/security.ts (RateLimiter class — token bucket algorithm)
    - .planning/phases/01-core-bot-infrastructure/01-RESEARCH.md (Rate Limiter code example)
  </read_first>
  <action>
1. Create `internal/security/ratelimit.go`:

   ```go
   package security
   ```

   `type ChannelRateLimiter struct`:
   - `mu sync.Mutex`
   - `limiters map[int64]*rate.Limiter`
   - `limit rate.Limit`
   - `burst int`

   `func NewChannelRateLimiter(requestsPerWindow int, windowSeconds int) *ChannelRateLimiter`:
   - Compute `r := rate.Limit(float64(requestsPerWindow) / float64(windowSeconds))`
   - Return struct with `limiters: make(map[int64]*rate.Limiter)`, `limit: r`, `burst: requestsPerWindow`

   `func (crl *ChannelRateLimiter) Allow(channelID int64) (bool, time.Duration)`:
   - Lock mutex
   - Get or create `rate.NewLimiter(crl.limit, crl.burst)` for channelID
   - Unlock mutex
   - Call `l.Reserve()`
   - If `!r.OK()`: return `false, 0`
   - If `r.Delay() > 0`: call `r.Cancel()`, return `false, r.Delay()`
   - Return `true, 0`

2. Create `internal/security/ratelimit_test.go`:
   - `TestRateLimiterAllow`: Create limiter(5, 60). First 5 calls to Allow(channelID=1) should return `(true, 0)`. 6th call should return `(false, delay>0)`.
   - `TestRateLimiterPerChannel`: Channel 1 exhausted, Channel 2 should still allow. Verify independence.
   - `TestRateLimiterConcurrent`: 10 goroutines calling Allow on same channel — no panic, no race. Run with `-race`.
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele &amp;&amp; go test ./internal/security/ -run TestRateLimiter -v -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/security/ratelimit.go contains `type ChannelRateLimiter struct`
    - internal/security/ratelimit.go contains `func NewChannelRateLimiter(`
    - internal/security/ratelimit.go contains `func (crl *ChannelRateLimiter) Allow(`
    - internal/security/ratelimit.go contains `rate.NewLimiter`
    - internal/security/ratelimit.go contains `sync.Mutex`
    - internal/security/ratelimit_test.go contains `TestRateLimiterPerChannel`
    - internal/security/ratelimit_test.go contains `TestRateLimiterConcurrent`
    - `go test ./internal/security/ -run TestRateLimiter -race -count=1` exits 0
  </acceptance_criteria>
  <done>Per-channel rate limiter works correctly with token bucket, channels are independent, concurrent access is safe</done>
</task>

<task type="auto">
  <name>Task 2: Create path validation, command safety, and auth check</name>
  <files>internal/security/validate.go, internal/security/validate_test.go</files>
  <read_first>
    - src/security.ts (isPathAllowed, isCommandSafe functions)
    - src/config.ts (BLOCKED_PATTERNS list, ALLOWED_PATHS)
    - .planning/phases/01-core-bot-infrastructure/01-CONTEXT.md (auth rejection message wording)
  </read_first>
  <action>
1. Create `internal/security/validate.go`:

   `func ValidatePath(path string, allowedPaths []string) bool`:
   - Normalize: `cleanPath := filepath.Clean(path)`
   - Convert to forward slashes for consistent comparison: `cleanPath = filepath.ToSlash(cleanPath)`
   - For each allowed path: normalize same way, check `strings.HasPrefix(cleanPath, allowedPath)`
   - Return true if any prefix matches, false otherwise

   `func CheckCommandSafety(text string, blockedPatterns []string) (bool, string)`:
   - Convert text to lowercase: `lower := strings.ToLower(text)`
   - For each pattern in blockedPatterns: if `strings.Contains(lower, strings.ToLower(pattern))`: return `false, pattern`
   - Return `true, ""`

   `func IsAuthorized(userID int64, allowedUsers []int64) bool`:
   - Linear scan: if userID matches any entry, return true
   - Return false
   - Note: Phase 1 is single-channel with user allowlist. Phase 2 will add per-channel membership checks.

2. Create `internal/security/validate_test.go`:
   - `TestValidatePathAllowed`: Path "/home/user/projects/foo.txt" with allowed=["/home/user/projects"] returns true
   - `TestValidatePathBlocked`: Path "/etc/passwd" with allowed=["/home/user/projects"] returns false
   - `TestValidatePathNormalization`: Path "/home/user/projects/../../../etc/passwd" with allowed=["/home/user/projects"] returns false (traversal attack)
   - `TestValidatePathWindows`: Path `C:\Users\me\projects\foo.txt` with allowed=[`C:\Users\me\projects`] returns true (Windows paths)
   - `TestCheckCommandSafetyBlocked`: Text "sudo rm -rf /" with standard blocked patterns returns (false, "sudo rm")
   - `TestCheckCommandSafetyAllowed`: Text "ls -la" returns (true, "")
   - `TestCheckCommandSafetyCaseInsensitive`: Text "FORMAT C:" matches "format c:"
   - `TestIsAuthorizedYes`: userID=123 in allowed=[123, 456] returns true
   - `TestIsAuthorizedNo`: userID=789 in allowed=[123, 456] returns false
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele &amp;&amp; go test ./internal/security/ -run "TestValidate|TestCheckCommand|TestIsAuthorized" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/security/validate.go contains `func ValidatePath(`
    - internal/security/validate.go contains `func CheckCommandSafety(`
    - internal/security/validate.go contains `func IsAuthorized(`
    - internal/security/validate.go contains `filepath.Clean`
    - internal/security/validate.go contains `strings.HasPrefix`
    - internal/security/validate_test.go contains `TestValidatePathNormalization`
    - internal/security/validate_test.go contains `TestCheckCommandSafetyCaseInsensitive`
    - `go test ./internal/security/ -run "TestValidate|TestCheckCommand|TestIsAuthorized" -count=1` exits 0
  </acceptance_criteria>
  <done>Path validation blocks traversal attacks, command safety catches all blocked patterns, auth check works, all tests pass</done>
</task>

</tasks>

<verification>
- `go test ./internal/security/... -v -race -count=1` passes
- Rate limiter isolates channels correctly
- Path traversal attacks are blocked
</verification>

<success_criteria>
- Rate limiter enforces per-channel token bucket
- Path validation normalizes and prefix-checks
- Command safety catches all blocked patterns case-insensitively
- Auth check works with user ID list
- All tests green with race detector
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-bot-infrastructure/01-03-SUMMARY.md`
</output>
