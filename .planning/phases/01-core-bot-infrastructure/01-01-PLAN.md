---
phase: 01-core-bot-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - .env.example
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/audit/log.go
  - internal/audit/log_test.go
autonomous: true
requirements: [CORE-02, CORE-06, DEPLOY-03]

must_haves:
  truths:
    - "Bot loads all required config from environment variables"
    - "Bot resolves claude CLI path at startup and logs it"
    - "Bot writes append-only audit log entries with timestamp, user, channel, action"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition with all dependencies"
      contains: "github.com/PaulSonOfLars/gotgbot/v2"
    - path: "internal/config/config.go"
      provides: "Environment parsing and path resolution"
      exports: ["Config", "Load"]
    - path: "internal/audit/log.go"
      provides: "Goroutine-safe append-only audit logger"
      exports: ["Logger", "New"]
  key_links:
    - from: "internal/config/config.go"
      to: "os.Getenv"
      via: "environment variable parsing"
      pattern: "os\\.Getenv"
    - from: "internal/config/config.go"
      to: "exec.LookPath"
      via: "fallback CLI path resolution"
      pattern: "exec\\.LookPath"
---

<objective>
Initialize the Go module with all dependencies and build the config and audit subsystems.

Purpose: Every other package depends on config (env vars, paths, constants) and audit logging. This must exist first.
Output: go.mod with all dependencies, config package, audit package, .env.example
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-bot-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and create config package</name>
  <files>go.mod, go.sum, .env.example, internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - src/config.ts (TypeScript config reference — env vars, path resolution, constants)
    - .planning/phases/01-core-bot-infrastructure/01-RESEARCH.md (stack decisions, architecture patterns)
  </read_first>
  <action>
1. Initialize Go module:
   ```
   go mod init github.com/user/gsd-tele-go
   go get github.com/PaulSonOfLars/gotgbot/v2
   go get golang.org/x/time
   go get github.com/rs/zerolog
   go get github.com/joho/godotenv
   ```

2. Create `.env.example` with these exact keys:
   ```
   TELEGRAM_BOT_TOKEN=
   TELEGRAM_ALLOWED_USERS=123456789
   CLAUDE_WORKING_DIR=C:\Users\you\projects
   CLAUDE_CLI_PATH=
   ALLOWED_PATHS=C:\Users\you\projects,C:\Users\you\Documents
   OPENAI_API_KEY=
   RATE_LIMIT_ENABLED=true
   RATE_LIMIT_REQUESTS=20
   RATE_LIMIT_WINDOW=60
   AUDIT_LOG_PATH=
   DATA_DIR=./data
   ```

3. Create `internal/config/config.go` with a `Config` struct containing these fields:
   - `TelegramToken string` — from `TELEGRAM_BOT_TOKEN`, required (panic if empty)
   - `AllowedUsers []int64` — from `TELEGRAM_ALLOWED_USERS`, comma-separated, required (panic if empty)
   - `WorkingDir string` — from `CLAUDE_WORKING_DIR`, default `os.UserHomeDir()`
   - `ClaudeCLIPath string` — from `CLAUDE_CLI_PATH`, fallback `exec.LookPath("claude")`, fallback `"claude"`
   - `AllowedPaths []string` — from `ALLOWED_PATHS`, comma-separated, default `[WorkingDir]`
   - `OpenAIAPIKey string` — from `OPENAI_API_KEY`, optional
   - `RateLimitEnabled bool` — from `RATE_LIMIT_ENABLED`, default `true`
   - `RateLimitRequests int` — from `RATE_LIMIT_REQUESTS`, default `20`
   - `RateLimitWindow int` — from `RATE_LIMIT_WINDOW`, default `60` (seconds)
   - `AuditLogPath string` — from `AUDIT_LOG_PATH`, default `filepath.Join(os.TempDir(), "claude-telegram-audit.log")`
   - `DataDir string` — from `DATA_DIR`, default `"./data"`
   - `SafetyPrompt string` — built from AllowedPaths using `buildSafetyPrompt()` (same template as TypeScript)
   - `BlockedPatterns []string` — hardcoded slice matching TypeScript: `"rm -rf /", "rm -rf ~", "rm -rf $HOME", "rm -rf %USERPROFILE%", "sudo rm", ":(){ :|:& };:", "> /dev/sd", "mkfs.", "dd if=", "format c:", "del /s /q c:"`

   Constants (exported):
   - `TelegramMessageLimit = 4096`
   - `TelegramSafeLimit = 4000`
   - `StreamingThrottleMs = 500`
   - `ButtonLabelMaxLength = 30`
   - `QueryTimeoutMs = 180_000`
   - `MaxSessionHistory = 5`
   - `SessionQueueSize = 5`

   `func Load() (*Config, error)` loads `.env` via `godotenv.Load()` (ignore error if no .env), then reads all env vars, resolves paths. Log resolved `ClaudeCLIPath` via `zerolog` at startup.

   Path resolution order for `ClaudeCLIPath`:
   1. `os.Getenv("CLAUDE_CLI_PATH")` — if non-empty, use directly
   2. `exec.LookPath("claude")` — if found, use result
   3. Fallback to `"claude"` literal

   `func buildSafetyPrompt(paths []string) string` — builds the same safety prompt text as TypeScript `buildSafetyPrompt()`.

   Filter `CLAUDECODE` from subprocess env: add helper `func FilteredEnv() []string` that returns `os.Environ()` with any entry starting with `CLAUDECODE=` removed.

4. Create `internal/config/config_test.go`:
   - `TestLoadConfig`: Set `TELEGRAM_BOT_TOKEN` and `TELEGRAM_ALLOWED_USERS` env vars, call `Load()`, verify fields populated
   - `TestLoadConfigDefaults`: Set only required env vars, verify defaults (RateLimitEnabled=true, RateLimitRequests=20, etc.)
   - `TestResolvePaths`: Set `CLAUDE_CLI_PATH` env var, verify it takes precedence over LookPath
   - `TestFilteredEnv`: Set `CLAUDECODE=test` in env, call `FilteredEnv()`, verify no entry starts with `CLAUDECODE=`
   - `TestBuildSafetyPrompt`: Call with `[]string{"/path/a", "/path/b"}`, verify output contains both paths
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele &amp;&amp; go test ./internal/config/ -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module github.com/user/gsd-tele-go`
    - go.mod contains `github.com/PaulSonOfLars/gotgbot/v2`
    - go.mod contains `golang.org/x/time`
    - go.mod contains `github.com/rs/zerolog`
    - go.mod contains `github.com/joho/godotenv`
    - internal/config/config.go contains `type Config struct`
    - internal/config/config.go contains `func Load() (*Config, error)`
    - internal/config/config.go contains `func FilteredEnv() []string`
    - internal/config/config.go contains `exec.LookPath("claude")`
    - internal/config/config.go contains `StreamingThrottleMs = 500`
    - internal/config/config.go contains `MaxSessionHistory = 5`
    - internal/config/config_test.go contains `TestLoadConfig`
    - internal/config/config_test.go contains `TestFilteredEnv`
    - .env.example contains `TELEGRAM_BOT_TOKEN=`
    - .env.example contains `CLAUDE_CLI_PATH=`
    - `go test ./internal/config/ -count=1` exits 0
  </acceptance_criteria>
  <done>Go module initialized with all deps, config loads from env with correct defaults, .env.example documents all vars, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create audit log package</name>
  <files>internal/audit/log.go, internal/audit/log_test.go</files>
  <read_first>
    - src/utils.ts (TypeScript audit log implementation)
    - src/types.ts (AuditEvent interface)
    - internal/config/config.go (AuditLogPath field)
  </read_first>
  <action>
1. Create `internal/audit/log.go`:
   - `type Logger struct` with fields: `mu sync.Mutex`, `file *os.File`, `encoder *json.Encoder`
   - `func New(path string) (*Logger, error)` — opens file with `os.O_APPEND|os.O_CREATE|os.O_WRONLY`, creates `json.Encoder` on the file
   - `func (l *Logger) Log(event Event) error` — acquires mutex, writes JSON line, no newline needed (json.Encoder adds it)
   - `func (l *Logger) Close() error` — closes file

   `type Event struct`:
   - `Timestamp string `json:"timestamp"`` — ISO 8601
   - `Action string `json:"action"`` — "message", "auth", "tool_use", "error", "rate_limit"
   - `UserID int64 `json:"user_id"``
   - `Username string `json:"username,omitempty"``
   - `ChannelID int64 `json:"channel_id"``
   - `Message string `json:"message,omitempty"`` — first 100 chars of message text
   - `Error string `json:"error,omitempty"``

   `func NewEvent(action string, userID int64, channelID int64) Event` — creates Event with `Timestamp` set to `time.Now().UTC().Format(time.RFC3339)`

2. Create `internal/audit/log_test.go`:
   - `TestAuditLogWrite`: Create logger with temp file, log an event, read file, verify JSON contains "timestamp", "action", "user_id"
   - `TestAuditLogAppendOnly`: Log two events, verify file has two JSON lines (split by newline, each parses as valid JSON)
   - `TestAuditLogConcurrent`: Launch 10 goroutines each logging 10 events, verify no panic, file has 100 lines, each valid JSON
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele &amp;&amp; go test ./internal/audit/ -v -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/audit/log.go contains `type Logger struct`
    - internal/audit/log.go contains `type Event struct`
    - internal/audit/log.go contains `func New(path string)`
    - internal/audit/log.go contains `sync.Mutex`
    - internal/audit/log.go contains `json.Encoder`
    - internal/audit/log_test.go contains `TestAuditLogConcurrent`
    - `go test ./internal/audit/ -race -count=1` exits 0
  </acceptance_criteria>
  <done>Audit logger writes JSON lines, is goroutine-safe, and concurrent test passes with race detector</done>
</task>

</tasks>

<verification>
- `go test ./internal/config/ -v -count=1` passes
- `go test ./internal/audit/ -v -race -count=1` passes
- `go.mod` exists with all required dependencies
</verification>

<success_criteria>
- Go module compiles with all dependencies resolved
- Config loads from env vars with documented defaults
- Claude CLI path resolution works (env var > LookPath > fallback)
- Audit logger writes concurrent JSON safely
- All tests green with race detector
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-bot-infrastructure/01-01-SUMMARY.md`
</output>
